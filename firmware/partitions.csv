# MagicBand Reader - 16 MB flash, A/B OTA layout.
#
# Explicit offsets so the app slots land on the required 64 KB (0x10000)
# boundary. Partition table lives at 0x8000, so the first partition starts
# at 0x9000. Total must not exceed 0x1000000 (16 MB).
#
#   nvs      0x09000 + 0x10000 -> 0x19000   (64 KB: Wi-Fi creds + ~100 cards + config)
#   otadata  0x19000 + 0x02000 -> 0x1B000   (A/B boot selector)
#   phy_init 0x1B000 + 0x01000 -> 0x1C000
#   (pad 0x1C000..0x20000 to 64 KB-align the app slot)
#   ota_0    0x20000 + 0x300000 -> 0x320000 (3 MB; app is ~342 KB today)
#   ota_1   0x320000 + 0x300000 -> 0x620000 (3 MB)
#   storage 0x620000 + 0x9E0000 -> 0x1000000 (~9.9 MB LittleFS: WAVs + web page)
#
# The "storage" partition keeps the historical 'spiffs' subtype - LittleFS
# mounts by label (subtype-agnostic), so no offset/size change was needed; the
# on-flash format just changed from SPIFFS to LittleFS.
#
# Note: switching to this layout is a one-time USB reflash that wipes NVS.
# All future OTA updates preserve NVS (enrolled cards survive).
#
# Name,   Type, SubType, Offset,    Size,     Flags
nvs,      data, nvs,     0x9000,    0x10000,
otadata,  data, ota,     0x19000,   0x2000,
phy_init, data, phy,     0x1B000,   0x1000,
ota_0,    app,  ota_0,   0x20000,   0x300000,
ota_1,    app,  ota_1,   0x320000,  0x300000,
storage,  data, spiffs,  0x620000,  0x9E0000,
